/**
 * Single source of truth for all simulation content.
 * Used by SimulationPanel, SimCard, WhatYouLearned, DeepDive, TerminalGuide, GrafanaCallout.
 */

export const SIMULATIONS = {
  'kill-pod': {
    id: 'kill-pod',
    number: 1,
    label: 'Kill Random Pod',
    startHere: true,
    icon: 'Trash2',
    accentColor: 'red',
    objective: 'Self-healing and ReplicaSets',
    without: 'Without Kubernetes: the crashed process stays down until someone manually SSHs in and restarts it — usually discovered by an alert at 3am.',
    description: 'Deletes a running backend pod via the Kubernetes API. The Deployment controller immediately notices and creates a replacement.',
    terminal: {
      before: { cmd: 'kubectl get events -n kubelab -w', why: 'Streams every event the control plane fires. You will see: Killing → SuccessfulCreate → Scheduled → Pulled → Started — the full self-healing sequence in real time.' },
      also: { cmd: 'kubectl get pods -n kubelab -w', why: 'Open a second tab. Watch the dead pod disappear and a brand-new one spin up.' },
      after: { cmd: 'kubectl describe replicaset -n kubelab | grep -A 5 "Events:"', why: 'Shows the exact ReplicaSet event: "SuccessfulCreate — Created pod backend-xxxx"' },
    },
    watch: ['Events Feed: Killing → Scheduled → Started', 'Cluster Map: pod chip flashes, then reappears'],
    happening: 'A backend pod was deleted. Kubernetes is creating a replacement right now.',
    grafana: null,
    observe: { prompt: 'Before reading the explanation — what changed about the pod\'s name in your terminal?', reveal: 'The suffix is completely different. "backend-abc123" is gone forever — Kubernetes created an entirely new object. This is why the RESTARTS counter is 0 on the replacement: it has never crashed. Restarting and replacing are different things.' },
    learn: { explanation: 'The ReplicaSet controller constantly checks that actual pod count matches desired (2). The moment your pod was deleted, it created a replacement. This reconciliation loop runs forever.', production: 'Node crashes, OOM kills, and process crashes all trigger the same loop. With replicas > 1 and a readiness probe, your app survives any single pod death with zero downtime.' },
    observationWindowSeconds: 12,
    quiz: { q: 'Why did Kubernetes create a pod with a NEW name instead of restarting the old one?', options: ['To avoid DNS naming conflicts between pods', 'Pods are immutable — the ReplicaSet creates a replacement, it never restarts the original', 'The scheduler deleted the old pod\'s ID from memory', 'Kubernetes always rotates pod names for security reasons'], correct: 1, explanation: 'Pods are immutable objects. Once created, their spec never changes. When deleted, they\'re gone permanently. The ReplicaSet controller\'s job is to create a new pod — which is why every replacement has a random suffix.' },
    variant: { title: 'Kill ALL backend pods simultaneously', cmd: 'kubectl delete pods -n kubelab -l app=backend', what: 'With replicas: 2, both die at once. Does the app stay up? Watch the RESTARTS column. You\'ll see a brief gap — 2 replicas don\'t guarantee zero downtime if they\'re killed simultaneously.' },
    breakers: [
      { q: 'What if you only had replicas: 1 — is there any downtime during the kill?', a: 'Yes. With one replica, killing it leaves the Service with no endpoints until the new pod is Ready. Requests get connection refused or 503 during that window.' },
      { q: 'What if the readiness probe was missing? Would the new pod receive traffic before it\'s ready to handle it?', a: 'Yes. Without a readiness probe, Kubernetes adds the pod to Service endpoints as soon as the container starts. Traffic can hit it before it has opened a DB connection or loaded config — you get 5xx until the app is actually ready.' },
      { q: 'What if terminationGracePeriodSeconds was 0? What happens to HTTP requests mid-flight when the pod dies?', a: 'The process gets SIGKILL immediately. In-flight requests are cut off; clients see connection reset. A nonzero grace period lets the pod finish current requests (or the proxy drain) before exit.' },
    ],
    deepDive: {
      anatomy: [
        { step: 1, text: 'You call DELETE /api/v1/namespaces/kubelab/pods/<n>. API server sets deletionTimestamp on the pod object in etcd — the pod is NOT yet dead.' },
        { step: 2, text: 'Endpoints controller sees deletionTimestamp → immediately removes the pod from the Service endpoint list. Traffic stops flowing to it before it shuts down.' },
        { step: 3, text: 'Kubelet on the node detects deletionTimestamp → sends SIGTERM to PID 1. The container has terminationGracePeriodSeconds (default: 30s) to shut down cleanly.' },
        { step: 4, text: 'ReplicaSet controller\'s watch fires: actual count (1) ≠ desired count (2). It calls POST /pods to create a replacement — this happens in parallel with shutdown.' },
        { step: 5, text: 'Scheduler evaluates all nodes: available CPU/memory, taints, tolerations, affinity rules. It writes nodeName onto the new pod spec in etcd.' },
        { step: 6, text: 'Kubelet on the selected node pulls the image (likely cached), starts the container, runs the readiness probe. When probe passes, endpoints controller adds the pod back to the Service.' },
      ],
      verify: [
        { cmd: 'kubectl get pods -n kubelab -w', annotation: 'Watch state transitions in real time. You will see: Running → Terminating (deletionTimestamp set) → a new pod: Pending → ContainerCreating → Running. Total time: 3–10 seconds in a healthy cluster.' },
        { cmd: 'kubectl describe replicaset -n kubelab -l app=backend | grep -A 3 "Replicas:"', annotation: 'The ReplicaSet spec shows desired=2. During the gap, current may read 1. After recovery: "2 current / 2 desired" confirms convergence.' },
        { cmd: 'kubectl get events -n kubelab --sort-by=.lastTimestamp | tail -15', annotation: 'Events tell the full story: Killing (kubelet), SuccessfulCreate (replicaset-controller), Scheduled (scheduler), Pulled/Started (kubelet). Each line is a real control-plane action.' },
        { cmd: 'kubectl get endpoints -n kubelab backend -o yaml | grep -A 5 addresses', annotation: 'Confirm the new pod IP is in the endpoint list. If it\'s missing, the readiness probe failed — the service is routing to nothing.' },
      ],
      trap: 'terminationGracePeriodSeconds: 0 is common "optimization" advice — it makes deploys feel faster. In reality it sends SIGKILL immediately, cutting all in-flight HTTP requests. Set it to match your longest expected request duration (typically 30–60s) and add a preStop: exec: [sleep, "5"] to let the load balancer drain connections before SIGTERM fires.',
      defend: 'Always define a readiness probe. Use preStop hooks to drain connections. Set terminationGracePeriodSeconds > your maximum request timeout. Add a PodDisruptionBudget (minAvailable: 1) to prevent simultaneous pod deletion during rolling updates.',
    },
    interviewQuestion: '"What happens when a pod crashes?"',
    interviewAnswer: 'Kubernetes detects desired state (2 replicas) ≠ actual (1). The ReplicaSet controller creates a replacement — not a restart, a new pod with a new name. The Service routes traffic to the surviving replica the entire time; there\'s no downtime with replicas: 2 and a readiness probe.',
    next: 'drain-node',
  },

  'drain-node': {
    id: 'drain-node',
    number: 2,
    label: 'Drain Worker Node',
    startHere: false,
    icon: 'ServerOff',
    accentColor: 'orange',
    hasRestore: true,
    objective: 'Node maintenance and pod eviction',
    without: 'Without Kubernetes: you SSH to each server, manually stop every process, update load balancer configs, then coordinate with every team whose app ran on that node.',
    description: 'Marks a node as unschedulable (cordon) then evicts all non-DaemonSet pods. The scheduler immediately places them on healthy nodes.',
    terminal: {
      before: { cmd: 'kubectl get events -n kubelab -w', why: 'You will see eviction events for each pod as the drain progresses.' },
      also: { cmd: 'kubectl get nodes -w', why: 'Watch the drained node STATUS change to SchedulingDisabled.' },
      after: { cmd: 'kubectl get pods -n kubelab -o wide', why: 'Check the NODE column — all pods should now be on the remaining nodes.' },
    },
    watch: ['Cluster Map: node gets CORDONED banner, pods move to other nodes', 'Events Feed: Evicted entries appear for each pod'],
    happening: 'The node is being cordoned (unschedulable) and all pods are being evicted.',
    grafana: null,
    observe: { prompt: 'Run: kubectl get pods -n kubelab -o wide — which node are the backend pods on now?', reveal: 'Both should be on nodes other than the one you drained. The drained node shows SchedulingDisabled in kubectl get nodes. New pods only land there again after you uncordon it — until then it\'s invisible to the scheduler.' },
    learn: { explanation: 'Draining cordons the node so no new pods land there, then evicts pods gracefully. The scheduler replaces them on any node with sufficient capacity.', production: 'Every kernel patch, cloud provider maintenance, or node upgrade uses this pattern. Pair it with a PodDisruptionBudget to guarantee minimum availability during the drain.' },
    observationWindowSeconds: null,
    quiz: { q: 'If you drain a node without a PodDisruptionBudget, what is the worst-case scenario?', options: ['The node would reject the drain command with an error', 'Both backend replicas could be on the same node and evicted at once — zero replicas, brief downtime', 'The database would lose all its data permanently', 'The drain would take twice as long to complete'], correct: 1, explanation: 'Without pod anti-affinity + PDB, the scheduler may place both replicas on the same node (it saw capacity there). A single drain evicts both simultaneously. Your replicas: 2 provides zero protection in that case.' },
    variant: { title: 'Check if your replicas share a node (a hidden HA gap)', cmd: 'kubectl get pods -n kubelab -o wide | grep backend', what: 'Look at the NODE column. If both backend pods show the SAME node — you just discovered you\'re one drain away from complete downtime. Pod anti-affinity prevents this.' },
    breakers: [
      { q: 'What if both backend replicas were on the same node when you drained it?', a: 'Both get evicted. You get a short period with zero backend pods until the ReplicaSet schedules two new ones on the remaining node(s). Same as the cascading-failure case.' },
      { q: 'What if the postgres PVC used local (hostPath) storage instead of network storage?', a: 'The PV is tied to that node. When the node is drained, the pod cannot be rescheduled elsewhere because the volume is local. The replacement postgres pod stays Pending until the node is uncordoned or you use network storage.' },
      { q: 'What if you tried to drain both worker nodes at the same time?', a: 'Pods that can run only on workers (e.g. node-exporter is a DaemonSet) get evicted from both. Workloads that can run on the control plane may schedule there; the rest stay Pending until at least one worker is uncordoned.' },
    ],
    deepDive: {
      anatomy: [
        { step: 1, text: 'kubectl drain is two operations: cordon + a loop of evictions. Cordon: PATCH node spec.unschedulable=true. Scheduler immediately skips this node for all future pod placement.' },
        { step: 2, text: 'For each non-DaemonSet pod, kubectl posts an Eviction object to /api/v1/namespaces/<ns>/pods/<name>/eviction. This is NOT a raw delete — it goes through policy enforcement first.' },
        { step: 3, text: 'The API server checks all PodDisruptionBudgets. If evicting this pod would violate a PDB (e.g., minAvailable: 1 and this is the only Running pod), the API returns 429 Too Many Requests and the drain BLOCKS until another replica is Running.' },
        { step: 4, text: 'Without a PDB: eviction proceeds exactly like a graceful delete — deletionTimestamp set, SIGTERM sent, grace period respected.' },
        { step: 5, text: 'All evicted pods enter Pending simultaneously. The scheduler races to place them on remaining nodes. If those nodes lack capacity, pods sit Pending until capacity frees up.' },
        { step: 6, text: 'After all pods are evicted, the node shows SchedulingDisabled in kubectl get nodes. It stays cordoned until you run kubectl uncordon — Kubernetes will NOT automatically restore it.' },
      ],
      verify: [
        { cmd: 'kubectl get pods -n kubelab -o wide | grep <node-name>', annotation: 'Before drain: shows all pods on the target node. Run again after drain: those pods should have moved. If any are still there after 60s, describe them — they may be stuck due to PVC affinity.' },
        { cmd: 'kubectl get nodes', annotation: 'Look for "SchedulingDisabled" next to the drained node. STATUS column will show Ready,SchedulingDisabled. This is the cordon flag.' },
        { cmd: 'kubectl get pods -n kubelab | grep Pending', annotation: 'If pods are Pending after the drain, the remaining nodes may be out of capacity. Run: kubectl describe pod <pending-pod> -n kubelab and read the Events section — the scheduler will tell you exactly why it cannot place the pod.' },
      ],
      trap: 'Two replicas with no pod anti-affinity and no PodDisruptionBudget. The scheduler often places both replicas on the same node because it sees available capacity there. When you drain that node, both pods are evicted simultaneously — complete downtime despite having replicas: 2. Check with: kubectl get pods -n kubelab -o wide | grep backend — if both show the same NODE, you are one drain away from an outage.',
      defend: 'Add a PodDisruptionBudget: kubectl create pdb backend-pdb --selector app=backend --min-available 1 -n kubelab. Add pod anti-affinity with topologyKey: kubernetes.io/hostname to force spread across nodes. This guarantees the drain of any single node never drops you below 1 running replica.',
    },
    interviewQuestion: '"How do you do zero-downtime node maintenance?"',
    interviewAnswer: 'Cordon the node so no new pods land there, then evict existing pods with kubectl drain. The scheduler places replacements on healthy nodes. Use a PodDisruptionBudget (minAvailable: 1) so the API blocks evictions that would leave zero replicas. Use pod anti-affinity so both replicas don\'t end up on the same node.',
    next: 'cpu-stress',
  },

  'cpu-stress': {
    id: 'cpu-stress',
    number: 3,
    label: 'CPU Stress — Throttling',
    startHere: false,
    icon: 'Cpu',
    accentColor: 'yellow',
    activeWindowSeconds: 60,
    objective: 'CPU limits and throttling on your real backend pod',
    without: 'Without Kubernetes: a runaway process consumes all CPU on the server, slowing down every other process. No per-app limits exist.',
    description: 'Burns CPU inside a real backend pod for 60 seconds. That pod\'s CPU limit is 200m — the Linux CFS scheduler will throttle it hard. The pod stays alive but slows down. This is exactly what happens to your app in production when CPU is constrained.',
    terminal: {
      before: { cmd: 'watch -n 5 kubectl top pods -n kubelab', why: 'Leave this running — it refreshes every 5 seconds. After clicking, one backend pod will jump to ~200m CPU (the limit). The other backend pod stays at idle. This is the throttle in action.' },
      also: { cmd: 'kubectl get pods -n kubelab', why: 'Second tab. The backend pod stays Running the entire 60 seconds. CPU throttling does NOT restart the pod — only OOMKill does. This is a key distinction.' },
      after: { cmd: 'kubectl logs -n kubelab -l app=backend -f', why: 'You will see "CPU stress running… complete" entries from the pod that handled the request. The other backend pod\'s logs are silent — it was never involved.' },
    },
    watch: ['kubectl top pods: one backend pod pegged at ~200m (the limit)', 'The OTHER backend pod keeps serving requests normally — this is HA in action'],
    happening: 'A backend pod is burning CPU right now. Its 200m limit means the Linux scheduler is throttling it hard — the pod stays alive, just slow.',
    grafana: { panel: 'CPU Usage per Pod', what: 'One backend pod will plateau at 200m for 60s — that\'s the throttle ceiling. Throttling is invisible here (panel shows usage at ceiling, not requests denied).' },
    observe: { prompt: 'Run: kubectl top pods -n kubelab — what CPU value do you see for the stressed backend pod?', reveal: '~200m — exactly the CPU limit. The pod was requesting 2000m+ but the kernel paused it 90% of the time. kubectl top shows "what was allowed through the throttle", not "what was requested". That\'s why high throttle is invisible without Prometheus metrics.' },
    learn: { explanation: 'The backend pod tried to use 100% of a CPU core but was throttled to 200m (0.2 cores) by the Linux CFS scheduler. The process ran the whole time — it was just paused 80% of the time. No crash, no restart. Just latency.', production: 'High CPU throttle % in Prometheus means your limit is too low — the app runs slow even though the node has headroom. The fix is to raise the CPU limit or profile what\'s causing the spike.' },
    observationWindowSeconds: null,
    quiz: { q: 'CPU usage was pinned at the limit for 60 seconds, but the pod never restarted. Why?', options: ['Kubernetes automatically increased the CPU limit to compensate', 'CPU limits cause throttling (slowness), not crashes — only memory limits cause OOMKills', 'The readiness probe detected the spike and prevented a restart', 'The CPU limit was not actually enforced — it\'s just a soft suggestion'], correct: 1, explanation: 'CPU limits and memory limits behave completely differently. CPU limits throttle: the kernel pauses the process to enforce the quota — it slows down but keeps running. Memory limits OOMKill: the kernel sends SIGKILL and the process dies instantly. This asymmetry trips up almost everyone.' },
    variant: { title: 'Confirm CPU throttling is invisible to kubectl top', cmd: 'kubectl top pods -n kubelab', what: 'You\'ll see one backend pod at ~200m CPU. Looks normal, right? But the pod was trying to use 2000m and was frozen 90% of the time. kubectl top shows usage at the ceiling — not what was denied.' },
    breakers: [
      { q: 'What if there were no CPU limits at all — what happens to other pods on the same node?', a: 'The stress pod can use as much CPU as the node has. Other pods get less CPU and slow down or starve. Limits cap one workload so the node stays fair.' },
      { q: 'What if the CPU limit was 10m (very low) — would the pod crash or just be extremely slow?', a: 'It does not crash. The kernel throttles it: the process is still Running but gets very little CPU time. It will appear stuck or very slow. No OOM, no restart.' },
      { q: 'What if your app had a slow CPU leak — how would you notice it before users do?', a: 'Monitor CPU usage over time (e.g. Prometheus container_cpu_usage_seconds_total or kubectl top). Set alerts when usage trends up over hours. Without that, you notice when the node is saturated and everything slows.' },
    ],
    deepDive: {
      anatomy: [
        { step: 1, text: 'A Kubernetes Job creates a pod running stress --cpu 2. The Job controller guarantees completions: 1 — it will retry if the pod fails.' },
        { step: 2, text: 'stress forks 2 threads, each spinning in an infinite sqrt() loop — 100% CPU per thread, consuming up to 2 full cores of host CPU.' },
        { step: 3, text: 'Kubernetes enforces the 600m CPU limit via Linux cgroups (cpu.cfs_quota_us). The container is allowed 60ms of CPU time per 100ms scheduling period — 60% of one core.' },
        { step: 4, text: 'After the container exhausts its 60ms quota, the Linux CFS scheduler freezes ALL processes in the cgroup for the remaining 40ms. This is CPU throttling — not a crash, not an OOM. The process pauses.' },
        { step: 5, text: 'The container does NOT crash, does NOT restart. It runs at a throttled rate. The metric container_cpu_cfs_throttled_seconds_total climbs rapidly in Prometheus during this period.' },
        { step: 6, text: 'After 60 seconds, stress exits cleanly. The Job marks the pod Succeeded. The Job controller does NOT create another pod (completions: 1 satisfied). The pod lingers in Completed state until TTL cleanup.' },
      ],
      verify: [
        { cmd: 'kubectl get pods -n kubelab -w | grep cpu-stress', annotation: 'Watch: Pending → ContainerCreating → Running (for 60s) → Succeeded. The pod runs for exactly the duration you specified. If it exits sooner, check the job events.' },
        { cmd: 'kubectl top pods -n kubelab --sort-by=cpu | grep cpu-stress', annotation: 'Shows current CPU usage. You will see it pinned near 600m (the limit). This is MISLEADING — the pod is trying to use 2000m but is throttled to 600m. kubectl top shows usage, not throttling rate.' },
        { cmd: 'kubectl get jobs -n kubelab', annotation: 'Shows COMPLETIONS: 0/1 while running, 1/1 after success. Duration column shows how long it ran. If you see FAILED, the stress binary was OOMKilled or the image pull failed.' },
      ],
      trap: 'CPU throttling is invisible in kubectl top and in most basic monitoring setups. A pod maxing out its CPU limit looks identical to a healthy pod — both show "600m/600m". The symptom is high latency: requests take 2–5x longer because the container is frozen 40% of the time. Teams spend hours checking application code for a "bug" that is actually a resource limit set too low. You need Prometheus metric: rate(container_cpu_cfs_throttled_seconds_total[5m]) to see it.',
      defend: 'For latency-sensitive workloads (APIs, databases), avoid CPU limits entirely — use requests only for scheduling, leave limits unset. The Guaranteed QoS class (requests = limits) causes the most throttling. Use Burstable QoS (requests < limits) to allow brief spikes. Monitor throttle % and alert when it exceeds 25%.',
    },
    interviewQuestion: '"What\'s the difference between CPU and memory limits?"',
    interviewAnswer: 'CPU limits throttle: the kernel pauses the process to enforce the quota — the pod stays Running but slow. Memory limits OOMKill: the kernel sends SIGKILL with no warning — the container exits with code 137. Throttling is invisible in kubectl top; OOMKill shows up as restarts and Last State: OOMKilled.',
    next: 'memory-stress',
  },

  'memory-stress': {
    id: 'memory-stress',
    number: 4,
    label: 'Memory Stress — OOMKill',
    startHere: false,
    icon: 'HardDrive',
    accentColor: 'red',
    activeWindowSeconds: 20,
    objective: 'OOMKill on your real backend pod',
    without: 'Without Kubernetes: a memory-leaking process eventually consumes the server\'s RAM, triggering the OS OOM killer which may kill random unrelated processes to free memory.',
    description: 'Allocates memory inside a real backend pod in 50 MB chunks until it crosses the 256 Mi limit. The Linux kernel sends SIGKILL (exit code 137) with no warning. The pod restarts. The other backend replica keeps serving traffic.',
    terminal: {
      before: { cmd: 'kubectl get pods -n kubelab -w', why: 'Leave this running. Watch the RESTARTS column on a backend pod — it will increment when the OOMKill happens. The other backend pod stays at 0 restarts.' },
      also: { cmd: 'kubectl get events -n kubelab -w', why: 'Second tab. Watch for the OOMKilling event on a backend pod — not a separate stress pod. This is your actual application pod dying.' },
      after: { cmd: 'kubectl describe pod -n kubelab -l app=backend', why: 'Look under "Last State" for the killed pod. You will see: Reason: OOMKilled, Exit Code: 137. That\'s 128 + signal 9 (SIGKILL from the kernel).' },
    },
    watch: ['kubectl get pods -w: one backend pod STATUS → OOMKilled → Running (restarted)', 'kubectl describe pod: "Last State: OOMKilled, Exit Code: 137"'],
    happening: 'A backend pod is allocating memory in chunks right now. It will cross the 256 Mi limit and get killed by the kernel with no warning.',
    grafana: { panel: 'Memory Usage per Pod', what: 'Watch the line approach 256Mi then disappear — that\'s the OOMKill. Pod Restarts increments.' },
    observe: { prompt: 'Run: kubectl describe pod -n kubelab -l app=backend | grep -A 3 "Last State:" — what do you see?', reveal: 'Reason: OOMKilled, Exit Code: 137. Exit 137 = 128 + signal 9 (SIGKILL). The Linux kernel sent SIGKILL directly — Kubernetes only observed the exit code afterward. There is no warning, no graceful shutdown for an OOMKill.' },
    learn: { explanation: 'The backend pod crossed its 256 Mi memory ceiling. The kernel did not warn it — it sent SIGKILL instantly. Kubernetes recorded this as OOMKilled, incremented the restart count, and replaced the container. The PVC and other pods were untouched.', production: 'OOMKilled + rising restart count = memory leak or limit set too low. The fix is kubectl top pods to see actual usage, then either fix the leak or raise the limit. Never run without memory limits in production.' },
    observationWindowSeconds: null,
    quiz: { q: 'The pod\'s exit code was 137. What does this mean, and who sent the signal?', options: ['Exit 137 = application bug, caught and reported by Kubernetes', 'Exit 137 = 128 + 9 (SIGKILL), sent by the Linux kernel OOM killer — Kubernetes only observed it', 'Exit 137 = timeout error, sent by the kubelet after the grace period', 'Exit 137 = network failure, sent by the kube-proxy on the node'], correct: 1, explanation: 'Exit code 137 = 128 + signal 9 (SIGKILL). The Linux kernel\'s OOM killer sent SIGKILL directly to the process — not Kubernetes. Kubernetes only observed the exit code and labeled it OOMKilled. This is why there\'s no graceful shutdown: SIGKILL cannot be caught, blocked, or handled.' },
    variant: { title: 'Find the OOMKill evidence in the pod description', cmd: 'kubectl describe pod -n kubelab -l app=backend | grep -A 8 "Last State"', what: 'Look for: Reason: OOMKilled, Exit Code: 137. This forensic trace persists until the pod is replaced. In production, this is how you confirm an OOMKill hours after it happened.' },
    breakers: [
      { q: 'What if memory limits weren\'t set at all — what would happen to the node?', a: 'The pod can consume all node memory. The kubelet then evicts pods (often the same or other workloads) to reclaim memory. You get random OOMKills and Pending pods. Limits protect the node and other workloads.' },
      { q: 'What if the app had a slow memory leak over 8 hours — would anyone notice before it crashed?', a: 'Only if you scrape memory metrics and alert on growth. Otherwise the first sign is OOMKill and restarts. Track container_memory_usage_bytes over time and alert on sustained growth.' },
      { q: 'What if you set memory limits lower than memory requests — would Kubernetes allow it?', a: 'No. The Pod is invalid. Kubernetes rejects it: "memory limit cannot be less than memory request". So you cannot commit to more memory than you allow.' },
    ],
    deepDive: {
      anatomy: [
        { step: 1, text: 'The Job pod runs stress --vm 1 --vm-bytes 400M --vm-keep. It uses mmap() to continuously allocate and touch 400MB of memory pages — this triggers actual physical memory allocation, not just virtual.' },
        { step: 2, text: 'Linux tracks anonymous memory pages per cgroup via memory.limit_in_bytes. The container limit is 200MB.' },
        { step: 3, text: 'When the working set crosses ~200MB, the kernel\'s OOM killer activates. It selects the highest-scoring process (stress, score based on memory usage) and sends SIGKILL — signal 9.' },
        { step: 4, text: 'SIGKILL cannot be caught, blocked, or ignored. There is no graceful shutdown. No preStop hook fires. No cleanup. The process terminates instantly.' },
        { step: 5, text: 'Exit code = 137 = 128 + 9 (SIGKILL signal number). Kubelet detects the container exited, records the reason as OOMKilled in pod status LastState.' },
        { step: 6, text: 'Job spec has restartPolicy: Never (default for Jobs). The Job controller marks the pod Failed and records a failure. With backoffLimit: 0 (set in our simulation), the Job itself fails — no retry.' },
      ],
      verify: [
        { cmd: 'kubectl get pods -n kubelab -w | grep mem-stress', annotation: 'The pod runs for 2–5 seconds then transitions directly from Running to OOMKilled. It does NOT go through Terminating — SIGKILL bypasses the graceful termination path entirely.' },
        { cmd: 'kubectl describe pod -n kubelab <mem-stress-pod> | grep -A 8 "Last State:"', annotation: 'Look for: "Reason: OOMKilled" and "Exit Code: 137". This is the definitive confirmation. If you see Exit Code: 1, the process crashed for another reason — not a memory limit violation.' },
        { cmd: 'kubectl get events -n kubelab --sort-by=.lastTimestamp | grep -i oom', annotation: 'The events stream will show an OOMKilling event from the kubelet. This is the only place Kubernetes records WHY the container was killed — not in pod status alone.' },
      ],
      trap: 'The silent restart loop. With restartPolicy: Always (the default for Deployments, not Jobs), Kubernetes restarts an OOMKilled container immediately. If the memory leak is slow, the pod runs fine for 4–8 hours, then crashes. Kubernetes restarts it — memory resets to 0, and the cycle begins again. Restart count climbs: 1, 3, 7, 15. No alert fires because CPU and memory metrics look normal between crashes. The app is effectively unreliable but the monitoring shows "green". You only notice when a user reports intermittent errors or you happen to run kubectl get pods.',
      defend: 'Alert on restart count > 2 within 60 minutes using Prometheus: kube_pod_container_status_restarts_total. Set memory requests conservatively (idle usage) and limits at your measured peak + 20% buffer. Profile memory usage with heap snapshots — add a preStop hook that sends SIGQUIT to your process to trigger a heap dump before OOM hits. Never set memory limits lower than memory requests.',
    },
    interviewQuestion: '"What\'s the difference between CPU and memory limits?"',
    interviewAnswer: 'CPU limits throttle: the kernel pauses the process — the pod stays Running but slow. Memory limits OOMKill: the kernel sends SIGKILL (exit 137) with no warning. OOMKilled pods restart; restart count climbs. Alert on kube_pod_container_status_restarts_total and fix the leak or raise the limit.',
    next: 'db-failure',
  },

  'db-failure': {
    id: 'db-failure',
    number: 5,
    label: 'Database Failure',
    startHere: false,
    icon: 'Database',
    accentColor: 'red',
    hasRestore: true,
    objective: 'Stateful workloads and data persistence',
    without: 'Without Kubernetes: the database runs directly in a container with no PVC — if the container dies, every row of data inside it is gone. Recovery means a restore from backup.',
    description: 'Scales the Postgres StatefulSet to 0. The database pod terminates but the PersistentVolumeClaim (and all data) is preserved.',
    terminal: {
      before: { cmd: 'kubectl get events -n kubelab -w', why: 'Watch the Postgres pod receive a Killing event. StatefulSets do not auto-replace when scaled to 0.' },
      also: { cmd: 'kubectl get pods -n kubelab -w', why: 'Watch postgres-0 terminate. Unlike a Deployment pod, it will NOT be recreated.' },
      after: { cmd: 'kubectl get pvc -n kubelab', why: 'The PVC stays Bound even with 0 pods running. Your data is safe on the volume.' },
    },
    watch: ['Cluster Map: postgres-0 disappears from its node', 'Events Feed: Killing for postgres-0'],
    happening: 'Postgres StatefulSet scaled to 0. The pod is terminating. Your data is safe on the PVC.',
    grafana: null,
    observe: { prompt: 'Run: kubectl get pvc -n kubelab — what is the STATUS of postgres-data-postgres-0?', reveal: 'Bound. It was Bound the entire time, even with zero pods running. The volume exists as an independent Kubernetes object that outlives pods. Data lives on the PVC, not in the container — this is the entire purpose of PersistentVolumeClaims.' },
    learn: { explanation: 'Scaling to 0 terminates the pod but leaves the PVC intact. Your data survives on the underlying storage. Restoring scales to 1 and Postgres reattaches the same volume automatically.', production: 'Scale-to-zero is used for emergency stops, pre-migration locks, and backup windows. Apps must handle connection failure with retries — a DB crash looks identical from the app\'s perspective.' },
    observationWindowSeconds: 12,
    quiz: { q: 'The postgres-0 pod was completely deleted. Why was all the data still there when it came back?', options: ['Kubernetes automatically backed up the data before deleting the pod', 'The PersistentVolumeClaim (PVC) exists independently of the pod — data lives on the volume, not in the container', 'PostgreSQL replicated its data to another pod during the shutdown', 'The pod wasn\'t really deleted — it was just temporarily paused by the scheduler'], correct: 1, explanation: 'Pods are ephemeral — their local filesystem is destroyed on deletion. PersistentVolumeClaims are separate Kubernetes objects that outlive pods. The StatefulSet guarantees postgres-0 always gets the PVC named postgres-data-postgres-0. This is the fundamental difference between StatefulSets and Deployments.' },
    variant: { title: 'Verify the PVC stayed Bound while the pod was gone', cmd: 'kubectl get pvc -n kubelab', what: 'Even right now (after restore), postgres-data-postgres-0 shows STATUS: Bound. It was Bound the entire time — even when postgres-0 didn\'t exist. Your data was on the volume, not in the container.' },
    breakers: [
      { q: 'What if the backend application didn\'t retry database connections after Postgres came back?', a: 'Backend keeps returning 5xx until the process is restarted (e.g. rollout restart) or the next health check fails and the pod is killed. Retries and backoff let the app recover without a restart.' },
      { q: 'What if the PVC used local storage and the node it lives on also got drained?', a: 'The PV is bound to that node. The replacement postgres pod cannot attach the volume on another node and stays Pending. You need network storage (or re-use the same node) for drain to reschedule Postgres.' },
      { q: 'What if you had 3 Postgres replicas with streaming replication — would this simulation cause data loss?', a: 'Scaling to zero and back up only affects the one instance you scaled. With replication, failover would promote a replica; this lab simulates a single instance. Data loss depends on replication lag and how you bring the primary back.' },
    ],
    deepDive: {
      anatomy: [
        { step: 1, text: 'PATCH /apis/apps/v1/namespaces/kubelab/statefulsets/postgres {spec: {replicas: 0}}. StatefulSet controller receives the update from its informer.' },
        { step: 2, text: 'StatefulSet deletes pods in reverse ordinal order (postgres-1 before postgres-0 in a multi-replica setup). For single replica: postgres-0 gets deletionTimestamp.' },
        { step: 3, text: 'Kubelet sends SIGTERM to the postgres process. Postgres catches SIGTERM, performs a checkpoint (writes dirty pages to disk), closes all client connections, writes a clean shutdown record to WAL.' },
        { step: 4, text: 'CRITICAL: The PersistentVolumeClaim postgres-data-postgres-0 is NOT deleted. It stays Bound. Volume unmounts from the node. All client connections get "FATAL: connection reset by peer" or "could not connect to server: Connection refused".' },
        { step: 5, text: 'Any application without connection retry/circuit breaker logic starts returning HTTP 500 or 503. If the app caches a failed connection (common with connection pools that don\'t validate), it stays broken even after Postgres restores.' },
        { step: 6, text: 'Scale back to 1: StatefulSet creates postgres-0, scheduler places it, Kubelet mounts the SAME PVC on whatever node it lands on, Postgres starts and replays WAL to reach consistent state. Existing clients that retry will reconnect.' },
      ],
      verify: [
        { cmd: 'kubectl get pods -n kubelab -w | grep postgres', annotation: 'Watch postgres-0 go through: Running → Terminating (up to 30s for checkpoint) → disappears. After restore: Pending → ContainerCreating → Running. The restore takes longer than a pod kill because Postgres replays WAL.' },
        { cmd: 'kubectl get pvc -n kubelab', annotation: 'This is the key verification: the PVC remains Bound even while postgres-0 is gone. STATUS=Bound, VOLUME shows the underlying PV. Your data is on that volume, not in the pod. This is what makes StatefulSets different from Deployments.' },
        { cmd: 'kubectl get statefulset postgres -n kubelab -o yaml | grep -A 3 "replicas:"', annotation: 'Shows spec.replicas (desired) vs status.readyReplicas (actual). During failure: status.readyReplicas=0. After restore: 1. The StatefulSet is the desired-state contract — it guarantees postgres-0 comes back with its data.' },
        { cmd: 'kubectl logs postgres-0 -n kubelab | tail -20', annotation: 'After restore: look for "database system was shut down" (clean shutdown record found) and "database system is ready to accept connections". If you see "database system was not properly shut down" it means Postgres is running crash recovery — this adds startup time.' },
      ],
      trap: 'StatefulSet + local storage = node affinity trap. If postgres-0\'s PVC uses a local volume or hostPath (tied to a specific node), and that node gets drained, postgres-0 CANNOT be rescheduled — the PVC cannot move. The pod will be Pending indefinitely with scheduler message: "0/3 nodes are available: 1 node had taint node.kubernetes.io/unschedulable, 2 node(s) didn\'t match Pod\'s node affinity/selector." The database is down until you uncordon the original node. This has caused multi-hour database outages in production.',
      defend: 'Use network-attached storage (EBS, GCE PD, Azure Disk, NFS) for StatefulSets — pods can then be rescheduled to any node. For production databases, use managed services (RDS, Cloud SQL, PlanetScale) and connect via a Service — Kubernetes manages the connection string, the cloud provider manages the data. Implement application-level connection retry with exponential backoff. Never assume a database is always available.',
    },
    interviewQuestion: '"What is a StatefulSet and when do you use it?"',
    interviewAnswer: 'StatefulSet gives pods stable identity (ordinal name, stable network ID) and stable storage (each pod gets a PVC that survives pod deletion). Use it for stateful workloads that need ordered deploy/scale and durable storage — databases, queues. Data lives on the PVC, not in the container.',
    next: 'kill-all-pods',
  },

  'kill-all-pods': {
    id: 'kill-all-pods',
    number: 6,
    label: 'Cascading Pod Failure',
    startHere: false,
    icon: 'Trash2',
    accentColor: 'rose',
    objective: 'Downtime despite replicas — why replicas: 2 is not enough',
    without: 'Without Kubernetes: all instances are down simultaneously — someone has to SSH into each server and restart manually. Minutes to hours of downtime.',
    description: 'Deletes BOTH backend pods simultaneously. With replicas: 2, killing one leaves a healthy replica serving traffic. Killing both causes 5–15 seconds of real downtime — zero endpoints, requests fail. Kubernetes creates replacements immediately, but there is a gap.',
    terminal: {
      before: { cmd: 'kubectl get endpoints -n kubelab backend -w', why: 'This is where the downtime becomes visible. When both pods die, ENDPOINTS goes to <none>. Every request during that gap fails. Watch it refill when the new pods pass their readiness probes.' },
      also: { cmd: 'kubectl get pods -n kubelab -w', why: 'Watch both backend pods Terminating at the same time — unlike the single kill, there is no surviving replica. The RESTARTS column will both show 0 on the new pods (fresh containers).' },
      after: { cmd: 'kubectl get events -n kubelab --sort-by=.lastTimestamp | tail -15', why: 'You\'ll see two SuccessfulDelete events firing at the same time, followed by two SuccessfulCreate events. The gap between them is your downtime window.' },
    },
    watch: ['kubectl get pods -n kubelab -w: both pods Terminating at the same time', 'kubectl get endpoints -n kubelab backend: ENDPOINTS goes empty, then refills'],
    happening: 'ALL backend pods were deleted simultaneously. The Service has zero healthy endpoints right now — requests are failing. Kubernetes is creating replacements.',
    grafana: { panel: 'HTTP Request Rate', what: 'Error spike for 5–15 seconds when both pods die simultaneously.' },
    observe: { prompt: 'Run: kubectl get endpoints -n kubelab backend — how many IPs are listed? Are they the same IPs as before?', reveal: 'Two new IPs — different from the originals. The old pods are gone; these are fresh replacements that passed their readiness probes. During the gap when ENDPOINTS showed <none>, every request to backend returned "connection refused".' },
    learn: { explanation: 'Two replicas protect against one pod dying at a time. They don\'t protect against both dying simultaneously — which happens during a bad deployment rollout, a cluster-wide eviction, or a node running both replicas failing. PodDisruptionBudgets and pod anti-affinity are the actual protection.', production: 'The fix: pod anti-affinity (spread replicas across nodes) + PodDisruptionBudget (guarantee ≥1 replica during voluntary disruptions). replicas: 2 is necessary but not sufficient.' },
    observationWindowSeconds: 12,
    quiz: { q: 'You have replicas: 2. Both pods died simultaneously. Was there downtime?', options: ['No — Kubernetes routes traffic to the node itself while pods restart', 'No — the Service keeps a buffer of recent responses', 'Yes — both pods died simultaneously, endpoints went empty, requests failed until replacements were ready', 'No — the readiness probe prevents traffic from stopping during restart'], correct: 2, explanation: 'replicas: 2 protects against one pod dying at a time. When both die simultaneously, the Service has zero endpoints — requests fail immediately. There is no buffering. The protection is pod anti-affinity (spread across nodes) + PodDisruptionBudget (block evictions that would leave zero replicas).' },
    variant: { title: 'Watch endpoints go empty then refill', cmd: 'kubectl get endpoints -n kubelab backend -w', what: 'You\'ll see ENDPOINTS: <none> for 5–15 seconds, then two new IPs appear. That gap = real downtime. This is what happens during a bad deployment that crashes all pods.' },
    breakers: [
      { q: 'What if a bad deployment rollout crashed all new pods before rolling back — how long would downtime last?', a: 'Until you roll back or fix the image. The Deployment keeps trying to run the broken version. Downtime lasts as long as it takes to notice and run rollout undo or push a fix.' },
      { q: 'What if you had 5 replicas but no pod anti-affinity — could all 5 end up on the same node?', a: 'Yes. The scheduler does not spread by default. A single node failure (or drain) could take out all 5. Pod anti-affinity (or topology spread) spreads replicas across nodes/zones.' },
      { q: 'What if you had a PodDisruptionBudget with minAvailable: 1 — would kubectl delete pods -l app=backend work?', a: 'Yes, but one at a time. The PDB allows voluntary disruption as long as at least 1 pod stays. So the first delete succeeds; the second is blocked until the first replacement is Ready. Deleting both in one shot would violate the PDB for a moment; the API may allow it and the controller then recreates.' },
    ],
    deepDive: null,
    interviewQuestion: '"What is a PodDisruptionBudget?"',
    interviewAnswer: 'A PDB specifies minimum available or maximum unavailable replicas during voluntary disruptions (drains, evictions). It blocks evictions that would violate the budget — e.g. minAvailable: 1 prevents both replicas from being evicted at once. Use with pod anti-affinity for real HA.',
    next: 'fail-readiness',
  },

  'fail-readiness': {
    id: 'fail-readiness',
    number: 7,
    label: 'Readiness Probe Failure',
    startHere: false,
    icon: 'Eye',
    accentColor: 'purple',
    activeWindowSeconds: 120,
    objective: 'Silent degradation — Running pod receiving zero traffic',
    without: 'Without Kubernetes: there is no built-in mechanism to tell the load balancer "this server is alive but not ready" — traffic keeps hitting it, half your requests fail, dashboards show the server as healthy.',
    description: 'Makes one backend pod fail its readiness probe for 120 seconds. The pod stays Running — kubectl get pods shows Running, liveness passes, no restarts. But Kubernetes removes it from Service endpoints. The other pod handles all traffic. This is the most misunderstood Kubernetes behavior.',
    terminal: {
      before: { cmd: 'kubectl get endpoints -n kubelab backend -w', why: 'Leave this running. When the readiness probe fails on one pod, its IP disappears from endpoints. The other pod\'s IP stays. This is the clearest signal that readiness != liveness.' },
      also: { cmd: 'kubectl get pods -n kubelab', why: 'Check this AFTER clicking — the failing pod shows STATUS: Running with 0 restarts. It is not crashing. It is alive and failing its readiness probe intentionally.' },
      after: { cmd: 'kubectl describe pod -n kubelab -l app=backend | grep -A 10 "Conditions:"', why: 'Look for: Ready: False, ContainersReady: True. This is the key evidence — the container is healthy but the pod is not ready for traffic. These two conditions are independent.' },
    },
    watch: ['kubectl get pods -n kubelab: STATUS is Running (pod alive, not crashing)', 'kubectl get endpoints -n kubelab backend: only 1 IP (this pod removed)', 'kubectl describe pod <this-pod>: Ready=False but ContainersReady=True'],
    happening: 'One backend pod is returning 503 from its /ready endpoint. Kubernetes is removing it from the Service endpoints — it will receive zero traffic, but the pod stays Running.',
    grafana: null,
    observe: { prompt: 'Run: kubectl describe pod -n kubelab -l app=backend | grep -E "Ready:|ContainersReady:" — what combination do you see?', reveal: 'One pod shows Ready: False with ContainersReady: True. The container is running. The pod is not ready for traffic. These are two independent conditions — liveness (is the process alive?) and readiness (is it ready for requests?) are separate probes with entirely different consequences.' },
    learn: { explanation: 'Liveness and readiness are separate concepts. Liveness = is the process alive? (fail → restart). Readiness = is it ready to serve traffic? (fail → removed from endpoints, no restart). A pod can be Running but receiving zero requests if its readiness probe fails.', production: 'This is how you take a pod out of rotation without killing it — for blue/green deployments, graceful draining, or maintenance. It\'s also how misconfigured probes cause silent partial outages: the pod looks healthy in dashboards but isn\'t serving.' },
    observationWindowSeconds: null,
    quiz: { q: 'The pod showed STATUS: Running and had 0 restarts. But it received no traffic. How?', options: ['The liveness probe killed the pod and restarted it so fast we didn\'t notice', 'The readiness probe failed — Kubernetes removed the pod from Service endpoints without restarting it', 'The CPU limit throttled the pod so heavily it couldn\'t respond to requests', 'NetworkPolicy blocked traffic to this specific pod\'s IP'], correct: 1, explanation: 'Liveness and readiness are separate. Liveness failure → restart. Readiness failure → removed from endpoints, zero traffic, no restart. The pod stayed Running and passed liveness, but failed readiness. This is intentional: it\'s how you gracefully drain a pod without killing it.' },
    variant: { title: 'Confirm the pod is Running but has no traffic', cmd: 'kubectl describe pod -n kubelab <failing-pod> | grep -A 5 Conditions', what: 'You\'ll see: Ready: False, ContainersReady: True. The container is running, liveness passes, but readiness fails. Cross-reference with endpoints — this pod\'s IP is missing.' },
    breakers: [
      { q: 'What if the readiness probe was the same as the liveness probe — when would that cause problems?', a: 'If the app is slow but not dead, the same check can fail for both. Kubelet kills the container (liveness) instead of just removing it from Service (readiness). You get restarts instead of graceful removal. Use readiness for "can serve traffic" and liveness for "process is alive".' },
      { q: 'What if ALL backend pods failed their readiness probes simultaneously — what would users experience?', a: 'The Service has no ready endpoints. Every request gets 503 or connection refused until at least one pod passes readiness again. Same as cascading failure from the client\'s view.' },
      { q: 'What if the readiness probe checked a dependency (like the database) — what happens when the database goes down?', a: 'All backend pods fail readiness and are removed from endpoints. That is correct for "do not send traffic to me". But if the DB is down, no pod can be ready, so you get total 503 until the DB is back. Often readiness checks only the app process; dependency health is handled by circuit breakers or separate checks.' },
    ],
    deepDive: null,
    interviewQuestion: '"Readiness probe vs liveness probe?"',
    interviewAnswer: 'Liveness: is the process alive? Fail → restart the container. Readiness: is it ready for traffic? Fail → remove from Service endpoints, no restart. A pod can be Running (liveness OK) but not ready (readiness fail) — it gets zero traffic. Use readiness for "can serve" and liveness for "is it dead?".',
    next: null,
  },
};

export const SIM_ORDER = [
  'kill-pod',
  'drain-node',
  'cpu-stress',
  'memory-stress',
  'db-failure',
  'kill-all-pods',
  'fail-readiness',
];
